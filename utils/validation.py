def validate_project_name(name):
    # Valida se o nome do projeto é válido (tamanho, caracteres permitidos, etc.)

def format_response(data, status_code=200):
    # Formata a resposta em JSON com o código de status e dados especificados
  
